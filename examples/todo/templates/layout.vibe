<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{\$title}</title>
    <style>
        body { font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 0; padding: 0; background: #f4f4f5; color: #111827; }
        header { background: #111827; color: #f9fafb; padding: 1rem 1.5rem; }
        main { padding: 1.5rem; max-width: 720px; margin: 0 auto; }
        form { margin-bottom: 1rem; display: flex; gap: 0.5rem; flex-wrap: wrap; }
        form.inline-form { display: inline-block; margin: 0 0.25rem; }
        input[type="text"] { flex: 1; padding: 0.5rem 0.75rem; border-radius: 0.375rem; border: 1px solid #d4d4d8; }
        input[type="date"] { padding: 0.5rem 0.75rem; border-radius: 0.375rem; border: 1px solid #d4d4d8; }
        button { padding: 0.5rem 0.75rem; border-radius: 0.375rem; border: none; background: #2563eb; color: #f9fafb; cursor: pointer; }
        button.secondary { background: #6b7280; }
        table.dev_table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
        table.dev_table th, table.dev_table td { padding: 0.5rem 0.75rem; border-bottom: 1px solid #e5e7eb; text-align: left; vertical-align: top; }
        table.dev_table tr:nth-child(even) { background: #f9fafb; }
        .done { text-decoration: line-through; color: #6b7280; }
        .overdue { color: #b91c1c; }
        small { color: #6b7280; }
    </style>
</head>
<body>
<header>
    <h1>{\$title}</h1>
</header>
<main>
    @output('main')
</main>
</body>
</html>
